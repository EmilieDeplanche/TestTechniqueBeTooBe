{{ form_start(form) }}
    {{ form_row(form._token) }}
    {{ form_row(form.name) }}
    {{ form_row(form.startDate) }}
    {{ form_row(form.endDate) }}
    {{ form_row(form.requiredAge) }}
    {{ form_row(form.maxPeople) }}
    {{ form_row(form.currentPeople) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
